<!DOCTYPE html>
<html lang="en">
  <%- include('./partials/head',{title}) %>
  <body>
    <header><%- include('./partials/navbar') %></header>
    <main>
      <%- include('./partials/sidebar') %>
      <div class="container">
        <div class="row">
          <div class="col s12">
            <h1><%= genre.name %></h1>
          </div>
        </div>
        <div class="row">
          <div class="col s12">
            <div class="card red lighten-5">
              <div class="card-content center-align red-text darken-5">
                <p>Are you sure you want to delete this genre?</p>
              </div>
              <div class="card-actions center-align">
                <form action="" method="post">
                  <input
                    type="text"
                    value="<%= genre._id %>"
                    name="id"
                    hidden
                  />
                  <button
                    type="submit"
                    style="margin-bottom: 1rem"
                    class="btn red"
                    href="<%= genre.url %>/delete"
                  >
                    Delete Genre
                  </button>
                </form>
              </div>
            </div>
          </div>
        </div>
      </div>
    </main>
    <footer></footer>
  </body>
</html>
