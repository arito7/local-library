<!DOCTYPE html>
<html>
  <%- include('./partials/head',{title}) %>
  <body>
    <header><%- include('./partials/navbar') %></header>
    <main>
      <%- include('./partials/sidebar') %>
      <div class="container">
        <h1><%= title %></h1>
        <p>
          Welcome to <em>LocalLibrary</em>, a very basic Express website
          developed as a tutorial example on the Mozilla Developer Network.
        </p>
        <h1>Dynamic Content</h1>
        <% if (error) { %>
        <p>Error getting dynamic content.</p>
        <% } else { %>
        <table>
          <caption>
            <h5>The library has the following record counts</h5>
          </caption>
          <tbody>
            <tr>
              <th>Books</th>
              <td><%= data.book_count %></td>
            </tr>
            <tr>
              <th>Copies</th>
              <td><%= data.book_instance_count %></td>
            </tr>
            <tr>
              <th>Copies Available</th>
              <td><%= data.book_instance_count %></td>
            </tr>
            <tr>
              <th>Authors</th>
              <td><%= data.author_count %></td>
            </tr>
            <tr>
              <th>Genres</th>
              <td><%= data.genre_count %></td>
            </tr>
          </tbody>
        </table>
        <% } %>
      </div>
    </main>
    <footer></footer>
  </body>
</html>
